var n = prompt("Digite a quantidade de Linhas:");
var escada = '', escada1 = '';
for (var y =1; y<=n; y++){
 escada = escada + '* ';
  escada1 = escada1 + '*';
  if (y<n)
  console.log(escada)
  else
  console.log(escada1)
}
